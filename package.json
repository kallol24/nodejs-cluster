{
  "name": "nodejs-cluster",
  "version": "1.0.0",
  "description": "poc to implement nodejs cluster for scaling application on multiple cores",
  "main": "index.js",
  "scripts": {
    "start-dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "nodejs",
    "cluster",
    "node-cluster",
    "scaling"
  ],
  "author": "kallol",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "loadtest": "^5.2.0",
    "nodemon": "^2.0.20"
  }
}
